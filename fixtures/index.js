/**
 * Import tests from here so custom fixtures + expect stay in one place:
 * `import { test, expect } from '../fixtures/index.js';`
 */
export { test, expect, resolveUrl } from './base.fixture.js';
